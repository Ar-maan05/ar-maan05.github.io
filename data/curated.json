{
  "comment": "Every merged upstream PR, newest first, with the note shown in the ledger. highlight marks the default 'Highlights' view. The pipeline re-verifies every state daily and appends merges not listed here automatically (without a note).",
  "curated": [
    {"repo": "systemd/systemd", "number": 43714, "note": "Forced SipHash's round inline where gcc -O2 declined, making short-key hashing up to 6.6x faster across systemd's hashmaps."},
    {"repo": "systemd/systemd", "number": 43663, "note": "Three fixes on journalctl's per-entry read path, including a UTF-8 decoder that ran twice per byte. Output is byte-for-byte identical, 1.7x to 2.5x faster.", "highlight": true},
    {"repo": "systemd/systemd", "number": 43653, "note": "The console-width helper used the permissive UTF-8 decoder, so one invalid cell failed a whole table with a bogus out-of-memory error."},
    {"repo": "util-linux/util-linux", "number": 4621, "note": "column(1) mis-measured cells after any non-SGR escape sequence; the scan now stops at a real ECMA-48 CSI final byte."},
    {"repo": "systemd/systemd", "number": 43753, "note": "Fixed both overflow guards in the load-average parser: one was inverted and one ran after the shift, so values from 2^53 up wrapped to 0.00 instead of being rejected."},
    {"repo": "systemd/systemd", "number": 43650, "note": "One datagram with a bad checksum, sendable by anyone on the link, disabled resolved's mDNS, LLMNR or stub listener until restart.", "highlight": true},
    {"repo": "systemd/systemd", "number": 43608, "note": "A leaked reference let resolved free an mDNS service browser while its maintenance timer still used it: a use-after-free, fixed in one line.", "highlight": true},
    {"repo": "systemd/systemd", "number": 43589, "note": "Stopped unbounded buffer growth on truncated compressed streams, and added fuzzers that feed hostile input to every decompressor and to importd's qcow2 path."},
    {"repo": "coreos/afterburn", "number": 1294, "note": "ProxmoxVE configs with more than one nameserver left DNS unset at boot; now emits one dracut nameserver= per address, IPv6 bracketed."},
    {"repo": "coreos/afterburn", "number": 1295, "note": "Truncating a long multi-byte hostname split a UTF-8 character and panicked, failing afterburn-hostname.service at boot."},
    {"repo": "BerriAI/litellm", "number": 35455, "note": "Re-landed Anthropic-native /v1/models after a staging revert, so Claude Code's model picker populates through a litellm gateway."},
    {"repo": "systemd/systemd", "number": 43326, "note": "The line re-breaker dropped text before an embedded newline and returned entries spanning lines, which could emit uncommented lines in Varlink IDL output."},
    {"repo": "systemd/systemd", "number": 43333, "note": "An empty long option (--=VALUE) prefix-matched every option, and with only one defined was silently accepted as it. Now rejected."},
    {"repo": "lance-format/lance", "number": 7845, "note": "An NGRAM-indexed contains() with a needle under three characters returned zero rows; it now falls back to a full recheck."},
    {"repo": "systemd/systemd", "number": 43230, "note": "An off-by-one skipped the last offset a two-byte escape sequence can start at, so ellipsize() over-truncated strings containing ESC M."},
    {"repo": "systemd/systemd", "number": 43150, "note": "A newline in an ask-password prompt could inject a Socket= line and send the typed password to another path. Unsafe characters are now refused.", "highlight": true},
    {"repo": "systemd/systemd", "number": 43102, "note": "Prompts of 255+ bytes wrapped Plymouth's one-byte length field, so the rest of the prompt was parsed as protocol data. Now rejected with -EMSGSIZE."},
    {"repo": "systemd/systemd", "number": 43078, "note": "A truncated multi-byte character at the end of a string made the line breaker step past the NUL terminator and read out of bounds."},
    {"repo": "systemd/systemd", "number": 43079, "note": "\\u escapes accepted UTF-16 surrogates and produced invalid UTF-8 downstream; now rejected, matching the \\U form."},
    {"repo": "systemd/systemd", "number": 43077, "note": "hostname_cleanup() stripped only one trailing separator, so foobar-- came back as the invalid hostname foobar-."},
    {"repo": "lancedb/lancedb", "number": 3599, "note": "MRR fusion averaged only over the rankings a document appeared in, letting a single-system hit outrank a consensus pick."},
    {"repo": "systemd/systemd", "number": 42578, "note": "Fixed incorrect reboot/pending logic in sysupdate when --component= is used. First PR merged into systemd."},
    {"repo": "lightpanda-io/browser", "number": 2722, "note": "Implemented Browser.setDownloadBehavior for CDP-driven file download automation."},
    {"repo": "lance-format/lance", "number": 7246, "note": "Fixed incorrect evaluation of list-element docs in the FTS prefilter walk-the-allowlist branch."},
    {"repo": "lancedb/lancedb", "number": 3512, "note": "Replaced panic paths in the Bedrock embedding provider with typed error returns across serialization, API, and runtime failure modes."},
    {"repo": "lancedb/lancedb", "number": 3511, "note": "Fixed opaque AttributeError for bare List/Tuple generics in Pydantic schema conversion, raising a clear TypeError instead."},
    {"repo": "BerriAI/litellm", "number": 30273, "note": "Enabled Anthropic-native model discovery for Claude Code gateway deployments."},
    {"repo": "BerriAI/litellm", "number": 30387, "note": "Preserved cache_control headers for OpenAI-compatible custom endpoints, fixing silent cache bypass."},
    {"repo": "BerriAI/litellm", "number": 30272, "note": "Surfaced max_input_tokens and max_output_tokens on /v1/models for accurate client-side capacity planning."},
    {"repo": "lance-format/lance", "number": 7251, "note": "Critical fix: merge_insert silently dropped matched rows when the leading payload column was all-null.", "highlight": true},
    {"repo": "BerriAI/litellm", "number": 30020, "note": "Stopped a cancelled stream from stranding its max_parallel_requests slot, which had been pinning capped keys at their limit.", "highlight": true},
    {"repo": "BerriAI/litellm", "number": 29493, "note": "Added a proxy-wide setting to disable budget reservation."},
    {"repo": "lancedb/lancedb", "number": 3459, "note": "Moved embedding search onto a dedicated executor to stop event-loop stalls."},
    {"repo": "lightpanda-io/browser", "number": 2635, "note": "Completed file-input support, from FileList through DOM.setFileInputFiles."},
    {"repo": "lancedb/lancedb", "number": 3444, "note": "The same DataFusion capability carried into LanceDB's Rust core."},
    {"repo": "BerriAI/litellm", "number": 29483, "note": "Stopped budget enforcement on model-discovery and info routes."},
    {"repo": "python/cpython", "number": 150328, "note": "Build-system fix accepted into the reference Python implementation.", "highlight": true},
    {"repo": "lance-format/lance", "number": 6934, "note": "DataFusion expressions for merge-insert predicates in a production columnar format."},
    {"repo": "lightpanda-io/browser", "number": 2537, "note": "W3C File API surface implemented in Zig for an AI-agent headless browser.", "highlight": true}
  ]
}
